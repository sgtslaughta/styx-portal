api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: selkies-hub
    watch: true
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true
